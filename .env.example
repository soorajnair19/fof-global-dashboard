# Optional: GitHub raw URL for member data (so the dashboard shows latest without redeploy).
# Example: https://raw.githubusercontent.com/YOUR_USERNAME/fof-dashboard/main/data/members.json
# If unset, the app reads from local data/members.json (e.g. when running locally).
# NEXT_PUBLIC_DATA_URL=
